name: Deploy HRMS React App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      # --- CLEAN + FRESH CHECKOUT ---
      - name: Checkout Code (force clean)
        uses: actions/checkout@v4
        with:
          clean: true
          fetch-depth: 0

      # Debug: Confirm latest code (PowerShell)
      - name: Show latest files
        run: Get-ChildItem -Force

      # --- LOGIN TO DOCKER HUB ---
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # --- BUILD IMAGE (NO CACHE) WITH BUILD ARGS ---
      - name: Build Docker Image (React + Nginx)
        run: |
          docker build `
            --no-cache `
            --build-arg VITE_SUPABASE_URL="${{ secrets.VITE_SUPABASE_URL }}" `
            --build-arg VITE_SUPABASE_ANON_KEY="${{ secrets.VITE_SUPABASE_ANON_KEY }}" `
            --build-arg VITE_SECOND_SUPABASE_URL="${{ secrets.VITE_SECOND_SUPABASE_URL }}" `
            --build-arg VITE_SECOND_SUPABASE_ANON_KEY="${{ secrets.VITE_SECOND_SUPABASE_ANON_KEY }}" `
            --build-arg MICROSOFT_CLIENT_SECRET="${{ secrets.MICROSOFT_CLIENT_SECRET }}" `
            -f docker/Dockerfile `
            -t ${{ secrets.DOCKER_USERNAME }}/hrms-frontend:latest `
            .

      # --- PUSH IMAGE ---
      - name: Push Image to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/hrms-frontend:latest

      # --- STOP OLD CONTAINERS & REMOVE NETWORK ---
      - name: Stop Containers and Remove Network
        run: |
          docker compose -f docker/docker-compose.yml down --remove-orphans
          docker network prune -f
        continue-on-error: true

      # --- REMOVE OLD IMAGE TO FORCE FRESH PULL ---
      - name: Remove Old Local Image
        run: |
          docker image rm -f ${{ secrets.DOCKER_USERNAME }}/hrms-frontend:latest
        continue-on-error: true

      # --- PULL LATEST IMAGE ---
      - name: Pull Latest Image from Docker Hub
        run: |
          docker compose -f docker/docker-compose.yml pull

      # --- DEPLOY WITH FRESH IMAGE ---
      - name: Deploy with Docker Compose
        run: |
          docker compose -f docker/docker-compose.yml up -d --force-recreate